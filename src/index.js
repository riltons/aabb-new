// Ponto de entrada principal do sistema AABB
// Estrutura inicial para React (ou ajuste conforme framework escolhido)

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
